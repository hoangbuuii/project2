<nav class="navbar navbar-fixed-top navbar-default header">
  <div class="container">
    <%= link_to t("project_2"), root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <% if current_user.is_admin? %>
              <%= link_to t("admin_panel"), rails_admin_path %>
            <% end %>
          </li>
          <li><%= link_to t("users"), users_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t("profile"), current_user %></li>
              <li><%= link_to t("settings"), edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to t("log_out"), destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t("sign_up_now"), new_user_registration_path %></li>
          <li><%= link_to t("log_in"), new_user_session_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</nav>
