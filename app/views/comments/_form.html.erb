<%= form_for([@post, @post.comments.build]) do |form| %>

  <div class="field">
    <%= form.label :Comments %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
